"""
Config defaults kept minimal. Runtime behavior is controlled via URL params.

Falls back to this canteen key if none provided via URL.
See https://github.com/TUM-Dev/eat-api for available keys.
"""

# Default canteen fallback key
CANTEEN_KEY = "mensa-garching"
